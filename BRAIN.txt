================================================================================
BRAIN.txt  —  directives for the coding agent working on the allocator lane
================================================================================

This file is written by a reviewing model that reads the repo between your
tasks. It is the authority on what to do next.

HOW TO USE IT
  - Read this file before starting any task.
  - Re-read it after you finish a task, before asking what's next.
  -

================================================================================

================================================================================

================================================================================
CURRENT TASK
================================================================================

Batch run on 24 real corrections: 88% agreement (21/24). One genuine defect.

FIX FIRST — the allocator talks itself over the recurrence threshold.

  Row 21, recurrence=2, rationale said:
      "recurrence >= 2, third occurrence"
  Row 16, recurrence=2, rationale said:
      "recurrence 2 means this is the third occurrence"

  The threshold is 3. Both rows have 2. The model is reinterpreting the count
  to cross the line. Row 16 scored as AGREE, so a coincidental match is masking
  a systematic error — the metric looks fine while the gate is broken.

  The recurrence gate is the only thing making the weights lane non-arbitrary.
  If the model can argue past it, that lane has no policy behind it.

  Fix in allocator/prompt.txt:
    - State the test as arithmetic, not judgement: WEIGHTS requires
      recurrence >= 3. recurrence is a COUNT OF PRIOR OCCURRENCES, already
      computed. Do not reinterpret it, do not add one for "this occurrence",
      do not reason about ordinals.
    - Say explicitly: recurrence 0, 1 or 2 CANNOT be weights, regardless of how
      behavioural the correction is. It goes to context and is promoted later
      when it recurs.
    - Require the rationale to quote the number when the weights lane is chosen.

  Then re-run: python -m allocator.batch labels.csv
  Row 21 must become context. Row 16 must become context (it is currently
  labelled weights in the CSV and both the label and the allocator are wrong —
  leave the CSV alone, just confirm the allocator now says context).

  Do NOT special-case these rows. Fix the rule.

WHY THIS MATTERS BEYOND THE NUMBER
  Row 21 is the demo's best frame: the same correction routes to context at
  recurrence 2 and to weights at 3. That only lands if the threshold is real.

THEN — lanes/weights/gate.py

  evaluate(incumbent: Adapter | None, challenger: Adapter) -> GateVerdict

  Score both adapters on a held-out slice of corrections, promote only if the
  challenger wins, return a verdict either way. Incumbent None means first
  adapter: promote it, note that in the verdict.

  The scoring function can be a stub that takes an adapter and returns a float —
  lanes/weights/ is not producing real adapters yet and may not today. Build the
  decision logic and the verdict, not the model evaluation. Make a REJECTION
  easy to trigger and clearly visible; a rejected cycle is a demo asset.

DO NOT YET
  Dashboard. Anything under lanes/code/ or lanes/context/.

================================================================================
REVIEW NOTES
================================================================================

Batch on 24 real corrections, 88% (21/24). Confusion: weights->code x2,
context->weights x1. No errors, no unparseable rows.

TWO OF THE THREE DISAGREEMENTS, THE ALLOCATOR WAS RIGHT AND THE HUMAN LABEL
WAS WRONG:
  Row 9  (merge/deploy need fresh approval) -> allocator said code. Correct:
         an approval gate is a deterministic precondition.
  Row 23 (decline instead of reconstructing a value) -> allocator said code.
         Correct: "is this present verbatim" is a check that can hard-refuse.

  Do not tune the prompt toward these. The labels were wrong, not the policy.

THE DISTRIBUTION IS A FINDING, NOT A PROBLEM:
  weights 12, code 8, context 4. The lanes fan out, so the routing decision has
  real stakes. This was the open question that would have killed the idea.

STILL TRUE FROM EARLIER REVIEWS — do not regress:
  no fallback lane; prompt read at call time; strict parsing; temperature 0;
  no key in source.
