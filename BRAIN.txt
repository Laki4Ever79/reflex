================================================================================
BRAIN.txt  —  directives for the coding agent working on the allocator lane
================================================================================

This file is written by a reviewing model that reads the repo between your
tasks. It is the authority on what to do next.

HOW TO USE IT
  - Read this file before starting any task.
  - Re-read it after you finish a task, before asking what's next.
  -

================================================================================

================================================================================

================================================================================

================================================================================
CURRENT TASK
================================================================================

The recurrence fix worked — rows 16 and 21 now correctly say context and quote
the arithmetic. But two consecutive batch runs on IDENTICAL input, one minute
apart, at temperature=0, gave different answers:

    run A: 22/24   disagreements: 16, and one code->context
    run B: 21/24   disagreements: 10, 14, 16

Row 10 agreed in A and disagreed in B. The allocator is unstable on borderline
rows, and both flipping rows are ones where the CODE test and the RECURRENCE
test both fire:

  row 10  "no AI attribution in commits"    checkable AND recurrence 3
          -> went code in A, weights in B
  row 14  "ops tokens in system env"        checkable AND a policy fact
          -> went code in A, context in B

FIX — make step 1 terminal.

  The decision order already says CODE is checked first. It is not behaving as
  terminal: the model evaluates all three and picks, so when two tests fire the
  answer depends on sampling.

  In allocator/prompt.txt, restate step 1 as a gate that ENDS the decision:

    - Step 1 is answered first and alone: is there a deterministic function
      that can GUARANTEE this rule? Answer yes or no before considering
      anything else.
    - If yes, the lane is CODE. Stop. Do not evaluate recurrence. Do not
      consider whether it is also behavioural. Recurrence is irrelevant to a
      rule a function can guarantee.
    - Only if step 1 is NO does recurrence get consulted at all.
    - Require the rationale to state the step 1 answer explicitly, e.g.
      "no function can guarantee this, so step 1 is no; recurrence=5 >= 3".

  This is the whole point of an ordered check rather than a score. Right now it
  is behaving like a score.

VERIFY PROPERLY — one run is not evidence any more.
  Run the batch THREE times and report all three agreement rates plus which
  rows differ between runs. The target is not a higher number, it is the SAME
  rows every time. A stable 21/24 beats a fluctuating 23/24.

  If rows still flip after the prompt fix, say so and stop rather than tuning
  further — we will pin the demo rows instead.

DO NOT
  Special-case rows 10 or 14. Lower the temperature (already 0). Add a
  majority-vote wrapper or retry-until-stable loop — that hides instability
  instead of fixing it, and the dashboard would show a lie.

AFTER THAT
  lanes/weights/gate.py is written but unreviewed. Leave it; it gets reviewed
  next round.

================================================================================
REVIEW NOTES
================================================================================

Recurrence fix: CORRECT. Rows 16 and 21 both route to context and the
rationales now quote "recurrence=2 < 3". The arithmetic framing worked.

Stability: NOT VERIFIED, and now known to be a problem. Two runs, different
answers, same input, temperature 0. Any agreement number quoted from a single
run is noise at the +/- 1 row level. This matters beyond the metric: if a demo
row routes differently on stage than it did in rehearsal, that is the failure
everyone in the room will see.

Row 16 note: the allocator says context and the CSV says weights. The CSV is
wrong (recurrence=2). Leave the CSV alone — a known-wrong human label that the
allocator correctly disagrees with is worth more in the video than a corrected
one, and the confusion matrix should show it.
